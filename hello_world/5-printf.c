#include <stdio.h>
/**
*main - Entry point
*Description: printf(
*Return: Always 0 (Success)
*discreption -prints
*/
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
