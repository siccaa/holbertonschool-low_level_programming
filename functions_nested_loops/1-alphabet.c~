#include <stdio.h>
#include "main.h"

int _putchar(char c);

void print_alphabet(void)
{
    char alphabet[] = "abcdefghijklmnopqrstuvwxyz\n";
    for (int i = 0; i < 27; i++)
    {
        _putchar(alphabet[i]);
    }
}

int main(void)
{
    print_alphabet();
    return 0;
}

int _putchar(char c)
{
    return putchar(c);
}
